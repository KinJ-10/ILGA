
# Diagnostic output uses the DK virtual COM port.
CONFIG_UART_CONSOLE=y
# Keep the ILRAS1 multi-line capture atomic enough for offline parsing.
# Application INFO messages otherwise interleave with individual tone rows.
CONFIG_LOG_OVERRIDE_LEVEL=1
